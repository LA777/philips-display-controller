<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <!-- Embed symbols so single-file publish of pdc emits a single artifact (no stray .pdb). -->
    <DebugType>embedded</DebugType>
  </PropertyGroup>

</Project>
